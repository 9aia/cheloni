# Suggestions

- Rewrite to Valibot so we can make std schemas more extensible
- Bind the handler's `this` to the command manifest (so that calling `this.name` inside the handler returns the current command's name)
- Add command bequeathing
- Add positional bequeathing
