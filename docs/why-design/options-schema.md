# Options Schema

## Why Objects as Options Schema

**Flexibility**: Enables dynamic options with constraints. Schemas such as `z.record(z.string(), z.number())` validates arbitrary key-value pairs while ensuring values are numbers.
