# Options Schema

## Why Objects as Options Schema

**Flexibility**: Enables dynamic options with constraints. Schemas like `z.record(z.string(), z.number())` validate arbitrary key-value pairs while ensuring values are numbers.
